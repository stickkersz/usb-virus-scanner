__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.claude/
# runtime data dirs if run from source
logs/
reports/
quarantine/
# stray Windows-path dirs created when the default config runs off-Windows
C:*
build/build/
dist/
Output/
# Bundled ClamAV engine + virus DB — huge, Windows-only, license-bound.
# Populate locally with build\fetch-vendor.ps1; never commit.
vendor/
*.spec.bak
# frozen-build temp
/build/*.toc
