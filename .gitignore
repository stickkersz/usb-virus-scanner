__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.claude/
# runtime data dirs if run from source
logs/
reports/
quarantine/
build/build/
dist/
Output/
# Bundled ClamAV engine + virus DB — huge, Windows-only, license-bound.
# Populate locally with build\fetch-vendor.ps1; never commit.
vendor/
*.spec.bak
# frozen-build temp
/build/*.toc
